# Mapbox public token (URL-restricted: localhost:*, *.vercel.app)
# Generato dal dashboard https://account.mapbox.com/access-tokens/
# Quando vuoto: i componenti MapView / StaticMap fanno fallback ai mock SVG.
NEXT_PUBLIC_MAPBOX_TOKEN=
